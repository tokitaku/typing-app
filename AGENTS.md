# AGENTS.md

## 基本方針

- すべての出力は日本語で行う。
- 返答は「結論 → 理由 → 次ステップ」の順で構成する。
- 変更は最小単位にとどめ、依頼範囲外の修正は避ける。
- 既存のユーザー変更を勝手に巻き戻さない。

## リポジトリ概要

- このリポジトリは、英単語と英語短文を学ぶタイピング MVP。
- フロントエンドは `frontend/` 配下の Next.js 14。
- バックエンドは `backend/` 配下の FastAPI。
- 問題データ API は `GET /problems`。

## 主要コマンド

- 初回セットアップ: `npm run setup`
- 開発サーバー起動: `npm run dev`
- 開発サーバー停止: `npm run dev:down`
- フロントエンドテスト: `npm test`
- バックエンドテスト: `uv run --python .venv/bin/python pytest backend/tests`
- Lint: `npm run lint`
- ビルド確認: `npm run build`

## 作業ルール

- 実装を始める前に、目的、変更範囲、責務分割、テスト方針を含む設計を必ず整理する。
- 並列化できる作業は、サブエージェントに分担して調査・実装・検証を進める。
- 新規実装やバグ修正は、原則として失敗するテストを先に書いてから着手する。
- 実装は `Red → Green → Refactor` の順で進める。
- リファクタリング前は、関連テストを先に実行し、失敗がないことを確認する。
- 変更後は、影響範囲に応じてテスト・lint・build を実行して結果を確認する。
- コード例を提示する場合は、インラインコメントを入れる。
- 生成物や依存ディレクトリの直接編集は避ける。対象は主に `frontend/` と `backend/`。
- API 仕様やユーザー向け挙動を変えた場合は、必要に応じて `README.md` も更新する。

## TDD の進め方

- テストを書く前に、対象の振る舞い、入出力、境界条件を設計として明確にする。
- まず期待する振る舞いを 1 つのテストケースとして追加し、失敗を確認する。
- テストを通すための最小限の実装だけを加える。
- テスト成功後にのみリファクタリングを行い、完了後に再度テストを実行する。
- バグ修正では、再現テストを先に追加してから修正する。
- テスト追加、実装、レビューは分担してよいが、最終的なマージ条件は「失敗テストの解消」と「全関連テスト成功」とする。
- 一度に複数の振る舞いを広げず、小さいサイクルで進める。

## 設計ルール

- 設計なしで実装を開始しない。
- 設計では少なくとも、目的、対象ファイル、責務分割、データの流れ、失敗時の扱い、テスト観点を明確にする。
- 変更が複数ファイルにまたがる場合は、どのサブエージェントがどこを担当するかを実装前に決める。
- 設計で合意した責務を超える変更が必要になった場合は、先に設計を更新してから実装を続ける。

## ブランチ戦略

- 作業ブランチは `main` から作成し、1つの目的に対して1ブランチを原則とする。
- ブランチ名は `codex/<種別>/<短い要約>` を基本とし、種別は `feature` `fix` `refactor` `test` `docs` などで表す。
- 仕様変更、リファクタリング、テスト追加を同時に行う場合でも、レビューしにくくなるならブランチを分ける。
- マルチサブエージェントで並行作業する場合は、親ブランチの方針を固定し、担当ごとに衝突しにくい単位で作業を分担する。
- マージ前提は、設計の更新完了、関連テスト成功、必要なレビュー反映、不要な差分が含まれていないこととする。
- 直接 `main` にコミットしない。

## マルチサブエージェント運用

- 実装・レビュー・テスト・リファクタリングは、可能な限り別サブエージェントに役割分担して並行に進める。
- サブエージェントごとに担当ファイルや責務を明確に分け、同じファイルを不用意に重複編集しない。
- 実装担当は、失敗するテストの追加と、そのテストを通す最小実装に集中する。
- レビュー担当は、仕様逸脱、回帰リスク、境界条件、テスト不足を優先して確認する。
- テスト担当は、追加テストの妥当性、既存テストへの影響、実行結果の確認を担当する。
- リファクタリング担当は、全テスト成功後にのみ構造改善を行い、振る舞い変更を持ち込まない。
- 各サブエージェントは、自分が担当していない変更を巻き戻さず、他担当の変更を前提に調整する。
- 最終担当は、各サブエージェントの結果を統合し、矛盾や重複を解消したうえでテストを再実行してから反映する。

## 変更時の着眼点

- フロントエンドの出題・学習ロジックは `frontend/src/lib/` を優先して確認する。
- 保存処理や復習関連の挙動は `localStorage` 利用箇所を確認する。
- バックエンドの問題データや API 変更時は `backend/main.py` とテストの整合性を保つ。
- Docker ベースの開発手順を壊さないよう、`compose.yml` と `Dockerfile.dev` の前提を尊重する。
