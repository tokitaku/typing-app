{
  "name": "typing-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "cd frontend && npm ci && cd .. && uv sync --project backend",
    "dev": "docker compose up --build",
    "dev:down": "docker compose down",
    "openapi:generate": "uv run --project backend python -m backend.openapi --output backend/openapi.json",
    "dev:frontend:docker": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "lint": "cd frontend && npm run lint",
    "test": "cd frontend && npm run test"
  }
}
