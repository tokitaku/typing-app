[project]
name = "typing-app-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "alembic==1.14.0",
    "fastapi==0.115.2",
    "uvicorn==0.30.6",
    "sqlmodel==0.0.22",
    "psycopg2-binary==2.9.9",
]

[dependency-groups]
dev = [
    "pytest==8.3.3",
    "httpx==0.27.2",
]

[tool.uv]
package = false
